apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: github-actions-runner
  namespace: openshift-gitops
spec:
  project: gh-actions-runner # Ensure this matches the name of the AppProject created earlier
  source:
    repoURL: https://github.com/MooseStack/github-actions-runner.git
    targetRevision: main
    path: helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: gh-actions-runner #Update namespace if needed, also update in argocd/1-project.yaml destinations section
  syncPolicy:
    managedNamespaceMetadata: # Comment this line if the namespace is created outside of ArgoCD
      labels: # Comment this line if the namespace is created outside of ArgoCD
        app.kubernetes.io/managed-by: openshift-gitops # Comment this line if the namespace is created outside of ArgoCD
        argocd.argoproj.io/managed-by: openshift-gitops # Comment this line if the namespace is created outside of ArgoCD
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true # Comment this line if the namespace is created outside of ArgoCD
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: "*"
      kind: "Application"
      jsonPointers:
        # Allow manually disabling auto sync for apps, useful for debugging.
        - /spec/syncPolicy/automated
        # These are automatically updated on a regular basis. Not ignoring last applied configuration since it's used for computing diffs after normalization.
        - /metadata/annotations/argocd.argoproj.io~1refresh
        - /operation
    - group: ""
      kind: Secret
      name: github-actions-runner-secret
      jsonPointers:
          - /data
          - /stringData